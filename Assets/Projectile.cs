using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Projectile : MonoBehaviour 
{
   
    //private Rigidbody rb;

    //private void Awake() {
    //    rb = GetComponent<Rigidbody>();
    //}

    //public void ApplyForceWithOffset(Vector3 forceToAdd) {
    //    rb.AddForce(forceToAdd, ForceMode.Impulse);

        //if (rb != null) {
            //Transform user = GameObject.Find("Player Attach Point").GetComponent<Transform>();

            // Get the current forward direction of the user
            //Vector3 userForward = user.forward;

            // Calculate the corrected direction by accounting for orbiting movement
            //Vector3 correctedDirection = Quaternion.LookRotation(userForward) * Vector3.forward;

            //Vector3 forceVector = correctedDirection;

            //rb.AddForce(forceVector * impulseForce, ForceMode.Impulse);

        //}
    //}

}
